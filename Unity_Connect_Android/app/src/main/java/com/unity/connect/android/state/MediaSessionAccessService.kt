package com.unity.connect.android.state

import android.service.notification.NotificationListenerService

/** Grants read-only access to active media sessions after the user enables notification access. */
class MediaSessionAccessService : NotificationListenerService()
